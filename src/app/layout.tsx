import type React from "react";
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Beyond The Stars - Streetwear & Purpose",
  description:
    "Discover the story, fashion, and purpose behind Beyond The Stars by Star Mitchell. Shop tees, hats, socks, hoodies, and more while supporting lupus awareness.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
